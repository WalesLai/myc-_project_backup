#include <cstdlib>
#include <iostream>

using namespace std;

int main(int argc, char *argv[])
{
    char ch='z';
    cout<<ch<<" ASCII value is "<<(int)ch<<endl;//(int)ch 顯示ASCII的編碼 
    //大小寫不一樣 
    system("PAUSE");
    return EXIT_SUCCESS;
}
